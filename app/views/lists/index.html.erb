<%= render "navbar" %>

<div id="banner">
  <div id="banner-text">
    <h1>Watch List Manager</h1>
    <p>Never miss a good movie again</p>
  </div>
</div>

<div class="container">

    <h1>Your lists</h1>

<% if !List.all.empty? %>
  <div class="container">
    <div class="row">
      <% @lists.each do |list| %>
          <div class="col-3">
            <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cloudinary_url(list.photo.key, height: 300, width: 400, crop: :fit) %>')">
              <%= link_to list.name, list_path(list) %>
            </div>
          </div>
      <% end %>
    </div>
  </div>
<% end %>



    <h3>Add a new list</h3>

    <%= simple_form_for(@list) do |f|%>
      <%= f.input :name %>
      <%= f.input :photo, as: :file %>
      <%= f.submit %>
    <% end %>
</div>
